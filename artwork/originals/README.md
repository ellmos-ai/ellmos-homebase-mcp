# Original Artwork

This folder keeps source-resolution artwork for future reuse.

- `homebase-logo-original.png`: original generated Homebase MCP logo supplied by the project owner.

The public README now uses the central ellmos emblem (hosted in `ellmos-ai/.github`), so this logo is kept here only as source artwork. This `artwork/` folder is intentionally not included in the npm package file list.
