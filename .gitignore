# Local Homebase configuration and secrets
.env
.env.*
!.env.example
homebase.toml
config/homebase.toml
config/*.local.toml
*.local.toml
*.secret.toml
secrets.json
secrets.*.json
*secret*.json
credentials.json
credentials.*.json
token.json
tokens.json
*.token.json
*.token
*.secret
.npmrc
.pypirc
*recovery*codes*.txt

# Local runtime data
.homebase/
*.db
*.sqlite
*.sqlite3
*-protocoll.txt

# Private key and certificate material
*.pem
*.key
*.p12
*.pfx
*.kdbx
*.crt
*.cert
*.csr
id_rsa*
id_ed25519*
id_ecdsa*
id_dsa*

# Python build and test artifacts
__pycache__/
*.py[cod]
.pytest_cache/
.pytest_temp/
.pytest_tmp/
.pytest_tmp*/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
build/
dist/
*.egg-info/
.venv/
venv/

# Node/npm artifacts
node_modules/
*.tgz

# mcp-publisher login artifacts (credentials, never commit)
.mcpregistry_*

# Multi-Host synchronization conflicts
*conflicted copy*
* (kopie)*
* (Kopie)*
* (copy)*
* (Copy)*
*-WORKSTATION*
*-WORKSTATION-LG*
*_WORKSTATION*
*_WORKSTATION-LG*
*-WORKSTATION.*
*-WORKSTATION-LG.*
*-ASUS*
*-ASUS-GEI*
*-LAPTOP*
*-Mac Studio*
*-MacBook*
*-IDEAPAD*
*.sync-conflict-*
*.conflict
*-CONFLIT-*
*-conflict-*
*.sync-temp-*
CONFLICT_REVIEW_LOG*

# Multi-Agent and lock artifacts
LOCK
LOCK.*
*.lock
LOCK*.txt
LOCK.user.*
LOCK.until.*
LOCK.condition.*
LOCK.permissions.json
.automation-lock
uv.lock
!package-lock.json

# Wheel packaging and smoke test caches
.wheel-smoke/
wheelhouse/
.coverage.*
.hypothesis/
.nyc_output/
.turbo/
.tox/

# Temporary, merge and backup files
*.tmp
*.bak
*.swp
*.swo
*.orig
*.rej
*~

# Operating system files
Thumbs.db
.DS_Store
Desktop.ini
